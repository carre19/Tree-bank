# URL del backend, con /api al final.
# En desarrollo se puede dejar vacio: el front cae en http://localhost:3001/api.
# En Netlify hay que cargarla en Site configuration -> Environment variables,
# apuntando al servicio de Render, por ejemplo:
VITE_API_URL=https://tree-bank-api.onrender.com/api
